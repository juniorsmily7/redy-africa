export enum Role {
  ADMIN = 'admin',
  SUB_ADMIN = 'sub_admin',
  USER = 'user',
}

export enum TokenKey {
  USER_KEY = 'user',
  TOKEN = 'token',
  REF_CODE_KEY = 'ref_code',
}

export enum ConstNum {
  MAX_TIMER_COUNTER = 60,
  PER_PAGE = 15,
}
